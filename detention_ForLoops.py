# for loop is used to loop through a set of code for a specified number of times
#using the for loop and range() function
#range() returns a sequence of numbers, default starts at 0 and increments of 1, ending at a specfied number
#range() ends at the number before, i.e. range(6) would stop at 5 because it starts from 0
i = 0
for i in range(101):
  print(i,'I will not use Snapchat in class')